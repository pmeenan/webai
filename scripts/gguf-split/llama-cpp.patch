diff --git a/tools/CMakeLists.txt b/tools/CMakeLists.txt
index 9463a8e..802bf14 100644
--- a/tools/CMakeLists.txt
+++ b/tools/CMakeLists.txt
@@ -12,6 +12,7 @@ llama_add_compile_flags()
 # tools
 
 if (EMSCRIPTEN)
+    add_subdirectory(gguf-split)
 else()
     add_subdirectory(batched-bench)
     add_subdirectory(gguf-split)
diff --git a/tools/gguf-split/CMakeLists.txt b/tools/gguf-split/CMakeLists.txt
index 6a07bbf..ff40cc1 100644
--- a/tools/gguf-split/CMakeLists.txt
+++ b/tools/gguf-split/CMakeLists.txt
@@ -3,6 +3,20 @@ add_executable(${TARGET} gguf-split.cpp)
 target_link_libraries(${TARGET} PRIVATE llama-common llama ${CMAKE_THREAD_LIBS_INIT})
 target_compile_features(${TARGET} PRIVATE cxx_std_17)
 
+add_executable(webai-gguf-plan webai-gguf-plan.cpp)
+target_link_libraries(webai-gguf-plan PRIVATE ggml-base ${CMAKE_THREAD_LIBS_INIT})
+target_compile_features(webai-gguf-plan PRIVATE cxx_std_17)
+target_link_options(webai-gguf-plan PRIVATE
+    -sMODULARIZE=1
+    -sEXPORT_ES6=1
+    -sENVIRONMENT=worker
+    -sFORCE_FILESYSTEM=1
+    -sEXPORTED_RUNTIME_METHODS=FS,callMain
+    -sINVOKE_RUN=0
+    -sINITIAL_MEMORY=64MB
+    -sMAXIMUM_MEMORY=1024MB
+)
+
 if(LLAMA_TOOLS_INSTALL)
     install(TARGETS ${TARGET} RUNTIME)
 endif()
